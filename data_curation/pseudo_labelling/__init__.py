"""OCR pseudo-labelling: run OCR engines over line crops to manufacture training labels.

consensus_labelling      three engines + agreement tiering (the full pipeline)
single_engine_labelling  one engine, predictions attached as columns (a bulk pass)
"""
